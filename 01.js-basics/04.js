console.group('Null & Undefined');

//Null & Undefined datatype
let age_4;
console.log(age_4, age_4 + 3, `the age is ${age_4}`);

let nullAge = null;
console.log(nullAge, nullAge + 3, `the age is ${nullAge}`);

console.groupEnd();
